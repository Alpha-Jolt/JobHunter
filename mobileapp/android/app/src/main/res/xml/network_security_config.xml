<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for JobHunter Android.
  - Production: HTTPS only, no cleartext permitted.
  - Development only: localhost (10.0.2.2 = Android emulator host) allowed
    over HTTP so the dev API server can be reached without a certificate.
  This file is referenced by android:networkSecurityConfig in AndroidManifest.xml.
-->
<network-security-config>
    <!-- Default: trust system CAs only, no cleartext -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Dev exception: allow HTTP to Android emulator host (localhost) only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
