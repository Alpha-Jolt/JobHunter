[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[tool.black]
line-length = 100
target-version = ["py310"]
include = '\.pyi?$'
exclude = '''
/(
    \.venv
  | \.git
  | __pycache__
  | build
  | dist
)/
'''

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["scraper/tests", "ai_engine/tests", "orchestration/tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--cov=scraper --cov=ai_engine --cov-report=term-missing"
markers = [
    "exit_criteria: marks tests that verify phase exit criteria against a live database (deselect with '-m not exit_criteria')",
]

[tool.coverage.run]
omit = [
    ".venv/*",
    "scraper/.venv/*",
    "ai_engine/.venv/*",
    "*/tests/*",
]

[tool.flake8]
max-line-length = 100
extend-ignore = ["E203", "W503"]

